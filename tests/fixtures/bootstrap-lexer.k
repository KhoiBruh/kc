// bootstrap lexer coverage
struct Pair(left: i32, right: u32) {}
extern fn write(val data: u8*, val length: u64): bool;

fn main(): i32 {
    val ratio: f32 = 1.25e2;
    val wide: f64 = 2.5e-3;
    var value = 40;
    if (value <= 42 && true) {
        print("ok");
    } else {
        value += 2;
    }
    return value != 0 || false;
}
